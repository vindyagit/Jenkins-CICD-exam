# cast-service Database
apiVersion: apps/v1

kind: Deployment

metadata:

  name: cast-db

spec:

  selector:

    matchLabels:

      app: cast-db

  template:

    metadata:

      labels:

        app: cast-db

    spec:

      containers:

        - name: cast-db

          image: {{ .Values.castDb.image | quote }}

          env:

            - name: POSTGRES_USER

              value: "{{ .Values.castDb.POSTGRES_USER}}"

            - name: POSTGRES_PASSWORD

              value: "{{ .Values.castDb.POSTGRES_PASSWORD }}"

            - name: POSTGRES_DB

              value: "{{ .Values.castDb.POSTGRES_DB }}"

          volumeMounts:

            - name: data

              mountPath: /var/lib/postgresql/data

      volumes:

        - name: data

          persistentVolumeClaim:

            claimName: cast-db-pvc